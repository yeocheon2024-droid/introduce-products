# ERP 품목 관리 시스템 - 현재 상황 (2026-03-23)

## 배포 정보
- **GitHub**: https://github.com/yeocheon2024-droid/introduce-products
- **배포 URL**: https://yeocheon2024-droid.github.io/introduce-products/
- **배포 방식**: GitHub Pages (main 브랜치 자동 배포)

---

## 최근 작업 내역 (오늘)

### 버그 수정
- **페이지네이션 중복 표시 버그** (commit: 977a791)
  - 원인: `.mobile-only { display: none; }` 보다 `.pagination { display: flex; }` 가 CSS에서 나중에 선언되어 모바일 전용 페이지네이션이 PC에서도 보임
  - 해결: `.mobile-only`에 `!important` 추가

### 신규 기능
- **품목 관리 탭 - 전체 보기 버튼** (commit: 70f9218)
  - 검색바에 "전체 보기" 토글 버튼 추가
  - 클릭 시 페이지네이션 없이 전체 품목 스크롤
  - 다시 클릭 시 "페이지별 보기" (50개씩) 복귀

- **이미지 관리 탭 - 다운로드 기능** (commit: 70f9218)
  - 각 이미지 카드에 개별 "다운로드" 버튼 추가
  - "전체 다운로드" 버튼으로 업로드된 이미지 전체 다운로드 (0.4초 간격 순차 실행)

---

## 현재 구현 완료된 기능

| 탭 | 기능 | 상태 |
|----|------|------|
| 대시보드 | 통계, 최근 등록 품목 | ✅ 완료 |
| 품목 관리 | CRUD, 검색/필터, 페이지네이션, 전체 보기, PC/모바일 반응형 | ✅ 완료 |
| 매입처 | 등록/관리 | ✅ 완료 |
| 분류 체계 | 대분류/중분류 관리 | ✅ 완료 |
| 마진율 설정 | 카테고리별 마진율, 판매단가 미설정 품목 목록, 일괄 적용 | ✅ 완료 |
| 이미지 관리 | 일괄 업로드(드래그앤드롭), 목록 조회, 개별/전체 다운로드 | ✅ 완료 |
| 엑셀 업로드 | .xlsx/.xls/.csv 파싱, 미리보기 후 일괄 등록 | ✅ 완료 |
| 견적서 분석 | Claude AI 분석, 기존 품목 비교/매칭, 응답 잘림 자동 이어받기 | ✅ 완료 |
| ERP 내보내기 | 발주고 형식(39컬럼), 경영박사 형식(7컬럼) | ✅ 완료 |
| Supabase 설정 | 자동 연결(키 내장), 양방향 동기화 | ✅ 완료 |

---

## 데이터 현황
- **기본 등록 품목**: 189개 (`default-products.js`)
- **이미지**: 57개 PNG (`02 이미지파일/` 폴더) → Supabase Storage에 업로드 필요
- **매입처**: JG(자체매입), WI(원일푸드), WD(왔다식품), HN(해농)

---

## 이미지 관련 메모
- 이미지 URL: `{supabase_url}/storage/v1/object/public/product-images/{품목코드}.png`
- 이미지 교체 방법: 같은 파일명(품목코드.png)으로 재업로드하면 자동 덮어씌움
- `02 이미지파일/` 폴더의 57개 PNG를 이미지 관리 탭에서 일괄 업로드해야 함 (아직 미완료)

---

## 주요 기술 메모

### 단가 구조
- `cost` (매입단가): VAT 포함 금액
- `sell` (판매단가): VAT 포함 최종 판매가
- 과세 판매단가 = 매입단가 × (1 + 마진율/100) × 1.1
- 면세 판매단가 = 매입단가 × (1 + 마진율/100)

### 견적서 매칭 (70점 이상만 매칭)
- 100점: 이름+규격 완전 일치
- 95점: 이름+규격 합쳐서 일치
- 90~78점: 이름 포함 관계 + 규격 비교
- 75점: 핵심 키워드 전부 포함 + 규격 일치
- 70점: 이름 완전 일치 (규격 다름)

### Claude API 설정
- 모델: claude-sonnet-4-20250514
- max_tokens: 16384 (JSON 잘림 방지)
- 응답 잘림 시 최대 3회 자동 이어받기
- API 키: localStorage `erp_api_key` (보안상 코드 미포함)
